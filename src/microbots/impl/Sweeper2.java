package microbots.impl;

import java.awt.Color;
import microbots.Action;
import microbots.MicrobotProcessingUnit;
import microbots.State;

/**
 * Challenge: After completing {@link Sweeper1}, create a bot that moves in a similar fashion, but
 * when it encounters a friend, it moves one cell to the side before continuing in the direction it
 * was traveling.
 */
public final class Sweeper2 extends MicrobotProcessingUnit {
  @Override
  public Color color() {
    // Step 1: Select a color.
    return null;
  }

  @Override
  public Action getAction(State state) {
    // Step 2: Program the bot!
    return null;
  }
}
