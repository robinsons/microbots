package microbots.impl;

import java.awt.Color;
import microbots.Action;
import microbots.MicrobotProcessingUnit;
import microbots.State;

/**
 * Challenge: After completing {@link Looper1}, create a bot that moves in a similar fashion, but
 * the radius of its spiral increases by 1 each time it completes a revolution. In other words,
 * after completing the motion described in Looper1, it should then move 2 cells in each direction,
 * then 3, and so on, increasing by 1 each time it completes a revolution.
 */
public final class Looper2 extends MicrobotProcessingUnit {
  @Override
  public Color color() {
    // Step 1: Select a color.
    return null;
  }

  @Override
  public Action getAction(State state) {
    // Step 2: Program the bot!
    return null;
  }
}
